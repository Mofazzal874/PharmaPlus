<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Square border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <size android:width="100dp" android:height="100dp" />
            <stroke
                android:width="4dp"
                android:color="@color/mycolor2" />
        </shape>
    </item>

    <!-- Horizontal line for the plus sign -->
    <item
        android:top="48dp"
        android:bottom="48dp"
        android:left="16dp"
        android:right="16dp">
        <shape android:shape="rectangle">
            <size android:width="68dp" android:height="4dp" />
            <solid android:color="@color/black" />
        </shape>
    </item>

    <!-- Vertical line for the plus sign -->
    <item
        android:left="48dp"
        android:right="48dp"
        android:top="16dp"
        android:bottom="16dp">
        <shape android:shape="rectangle">
            <size android:width="4dp" android:height="68dp" />
            <solid android:color="@color/black" />
        </shape>
    </item>

</layer-list>
